/**
 * Shared types for all city landmark icon components.
 * Every SVG icon accepts these props for consistency.
 */
export interface CityIconProps {
    /** Icon width & height in dp. @default 48 */
    size?: number;
    /** Stroke / fill color. @default '#C0C0C0' */
    color?: string;
}
